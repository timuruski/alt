#!/usr/bin/env ruby

require 'pathname'

path = Pathname.new(ARGV[0])
extname = path.extname

if path.to_s =~ %r{_spec}
  puts path.sub('spec', 'lib').sub('_spec', '')
elsif path.to_s =~ %r{lib/}
  puts path.sub('lib', 'spec').sub_ext('_spec' + extname)
else
  puts path
end
